.wrapper
  .sign-up__form
    Minimumをはじめましょう
  = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
    = render "devise/shared/error_messages", resource: resource
    .field.user-name
      = f.label :name
      %br/
      = f.text_field :name, autofocus: true, autocomplete: "name", class: "user__field"

    .field.user-email
      = f.label :email
      %br/
      = f.email_field :email, autofocus: true, autocomplete: "email", class: "user__field"

    .field.user-password
      = f.label :password
      - if @minimum_password_length
        %em
          (#{@minimum_password_length} 文字以上)
      %br/
      = f.password_field :password, autocomplete: "new-password", class: "user__field"

    .field.user-password_confirmation
      = f.label :password_confirmation
      %br/
      = f.password_field :password_confirmation, autocomplete: "new-password", class: "user__field"

    .user-submit
      = f.submit "ユーザー登録をする", class: "user-submit__btn"
